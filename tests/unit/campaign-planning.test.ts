import { describe, expect, it } from 'vitest';
import { calculateCampaignProgress, calculateCampaignStage, calculateNextCampaignAction, type CampaignPlanningState } from '../../src/domain/campaign/planning';
const base:CampaignPlanningState={status:'PLANNING',sessionZeroDate:null,firstSessionDate:null,frequency:null,nextSessionDate:null,hasCharacters:false,sessionsCompleted:0,sessionGoal:null};
describe('planejamento da campanha',()=>{
  it.each([[base,'Sessão Zero','Agendar Sessão Zero'],[{...base,sessionZeroDate:'2026-01-01'},'Personagens','Definir personagens'],[{...base,sessionZeroDate:'2026-01-01',hasCharacters:true},'Primeira sessão','Agendar primeira sessão'],[{...base,sessionZeroDate:'2026-01-01',hasCharacters:true,firstSessionDate:'2026-01-02'},'Frequência','Definir frequência'],[{...base,sessionZeroDate:'2026-01-01',hasCharacters:true,firstSessionDate:'2026-01-02',frequency:'WEEKLY'},'Em andamento','Agendar próxima sessão'],[{...base,sessionZeroDate:'2026-01-01',hasCharacters:true,firstSessionDate:'2026-01-02',frequency:'WEEKLY',nextSessionDate:'2026-01-09'},'Em andamento','Preparar próxima sessão'],[{...base,status:'PAUSED'},'Pausada','Decidir retomada'],[{...base,status:'COMPLETED'},'Concluída','Registrar encerramento']] as Array<[CampaignPlanningState,string,string]>)('cobre cada branch',(state,stage,action)=>{expect(calculateCampaignStage(state)).toBe(stage);expect(calculateNextCampaignAction(state)).toBe(action);});
  it('calcula progresso sem inventar percentual',()=>{expect(calculateCampaignProgress(base)).toBeNull();expect(calculateCampaignProgress({...base,sessionGoal:8,sessionsCompleted:3})).toBe(38);expect(calculateCampaignProgress({...base,sessionGoal:2,sessionsCompleted:9})).toBe(100);expect(calculateCampaignProgress({...base,status:'COMPLETED'})).toBe(100);});
});

