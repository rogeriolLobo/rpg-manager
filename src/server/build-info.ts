// Regenerado automaticamente por scripts/generate-build-info.mjs antes de cada
// deploy (`npm run deploy`). Os valores abaixo são apenas o placeholder usado
// em desenvolvimento local e no CI — nunca o commit real de produção.
export const BUILD_COMMIT = 'dev';
export const BUILD_TIME = 'unknown';
