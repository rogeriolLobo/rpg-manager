# RPG Manager — Roadmap Completo Planejado

Auditoria de todo o roadmap real (não inventado) a partir de:
`docs/product/MASTER_BACKLOG.md`, `docs/product/RPG_MANAGER_FINAL_STATUS.md`,
`docs/audit/RPG_MANAGER_1_0_MATRIX.md`, `docs/audit/WORLDCRAFT_GAP_MATRIX.md`,
`.claude/CLAUDE.md` (seções 25–33), migrations e código atual.

Correção de direção do responsável do produto (2026-08-19): o congelamento
pós-1.0 está revogado. Meta agora: `RPG MANAGER — PLANNED_ROADMAP_COMPLETE`.
Itens abaixo que estavam listados como `OUT_OF_SCOPE_1_0` (Social, Sheets,
VTT) eram deliberadamente adiados **até a estabilização do 1.0** — não
cancelados. O 1.0 está estabilizado (`RPG_MANAGER_FINAL_STATUS.md`), então
esses itens voltam ao backlog ativo aqui.

IDs seguem a sequência já usada em `MASTER_BACKLOG.md` (F-001..F-015).

## Já concluído (F-001 a F-015, não refazer)

| ID | Feature | Status |
|---|---|---|
| F-001 | Revision History (Vault/Journal/Worlds) | `DONE` (BATCH5) |
| F-002 | Cartografia zero-cost (mapas/pins) | `DONE` (BATCH3) |
| F-003 | External Resources | `DONE` (BATCH2) |
| F-004 | GM Tools (dados, timer) | `DONE` (BATCH3) |
| F-005 | Ideas / Quick Capture | `DONE` (BATCH2) |
| F-006 a F-014 | (busca, invites, quests/handouts via Vault, compendium via Vault, split de domínio Library, capa+KV, Open Library, dedup ISBN, archive) | `DONE` |
| F-015 | Backup/Restore JSON completo | `IN_PROGRESS` (BATCH20 — reclassificado de `DONE` para `IN_PROGRESS`: "Backup/Restore completo" só é aceito quando o restore cobre TODOS os domínios persistentes relevantes, não apenas o export) — export v9 completo; restore agora cobre Worlds/Creature Stat Templates/Vault/Journal/world_entity_links/**Library/Groups/GroupMembers/Campaigns/CampaignMembers/Sessions/Attendance/Sheet Templates/Character Sheets/Wiki (pastas/tags/aliases)/Relations/Cartografia (mapas/pins)/External Resources/Timeline (eras/calendário/datas de eventos)/Adventures estruturadas (scenes/encounters/scene entities/handouts)/VTT (scenes/tokens/fog/combatants — estado ao vivo sempre restaurado inativo)/Social (friendships/blocks/invites — só quando quem restaura é uma das partes reais, nunca forja grafo social alheio)/Social Library Interest** (BATCH20); Revision History e Files (bytes) continuam exportados (metadata) sem restore automatizado — ver `RestorePlan` em `backup-restore.ts` |

## Roadmap planejado ainda não iniciado

| ID | FEATURE | STATUS_ATUAL | PLANEJADA? | DEPENDÊNCIAS | RISCO | MIGRATION? | BACKEND | FRONTEND | TESTES | STATUS |
|---|---|---|---|---|---|---|---|---|---|---|
| F-015 | Backup/Restore completo, versionado (`schemaVersion`) | `IN_PROGRESS` (BATCH20): export v9 cobre 100% dos domínios; restore agora cobre Worlds/Creature Stat Templates/Vault (+especializados)/Journal/world_entity_links (BATCH19) + Library/Groups/GroupMembers/Campaigns/CampaignMembers/Sessions/Attendance (BATCH20, com deduplicação de título/nome única ao vivo e preservação de referência a conta externa real quando ela ainda existe — nunca recriada) — Wiki/Relations/Cartografia/External Resources/Timeline/Revision History/Social/Sheets/Adventures estruturadas/Files/VTT exportados, restore automatizado pendente | Sim — `CLAUDE.md` §22 do pedido original de finalização | Nenhuma (todos os domínios já existem) | Médio — mitigado: restore sempre cria registros novos, nunca sobrescreve | Não (`backup_restore_jobs`, migration 0026, aditiva) | `DONE` | `DONE` | `DONE` (2 testes novos: round-trip completo Group→Campaign→Member→Session + EXTERNAL_DEPENDENCY) | `IN_PROGRESS` (restore dos domínios restantes segue como próximo incremento) |
| F-016 | Social — amizades (busca, pedido, aceitar, recusar, cancelar, remover, bloquear) | Implementado: `friend_requests`+`friendships`+`user_blocks` (migration 0027), busca reaproveita `GET /directory/users` existente (nunca expõe e-mail), pedido cruzado auto-aceita, bloquear remove amizade/pedido nos dois sentidos e impede novo pedido | Sim — `CLAUDE.md` §32 | F-015 (backup deve cobrir dados sociais desde o início — ver nota BATCH7 abaixo) | Alto — superfície de IDOR/privacidade nova, mitigada com testes dedicados | Sim — `friend_requests`,`friendships`,`user_blocks` (0027, aditiva) | `DONE` | `DONE` | `DONE` (6 integration + 2 E2E desktop/mobile) | `DONE` (BATCH7) |
| F-017 | Social + Biblioteca (visão parcial da Library de amigos, RPGs em comum, interesse social ≠ campo pessoal "Quero jogar") | Implementado: opt-in explícito (`user_preferences.library_visible_to_friends`, desligado por padrão), `rpg_social_interest` separado de `wants_to_play` (nunca exposto), `GET /social/friends/:userId/library` nunca retorna notes/priority/playGroupNotes/playGroupId/gameMaster/plannedPlayDate | Sim — `CLAUDE.md` §32 | F-016 (`DONE`) | Alto — mitigado: opt-in por padrão desligado, campos privados nunca no SELECT, testado explicitamente (achado real corrigido durante a rodada: 0/1 do SQLite vs `boolean` no teste, não é bug de produto) | Sim — `user_preferences.library_visible_to_friends`,`rpg_social_interest` (migration 0028, aditiva) | `DONE` | `DONE` | `DONE` (3 integration + parte do E2E) | `DONE` (BATCH8) |
| F-018 | Social + Grupos/Campanhas (convidar amigo, propostas de mesa, guests) | Implementado: `social_invites` (convite explícito, distinto do fluxo já existente de adicionar qualquer conta cadastrada direto), só amigo pode ser convidado, só dono do Grupo/Campanha convida, aceitar cria `play_group_members`/`campaign_members` vinculado (`user_id`) — remoção de membro (já existente) continua preservando histórico (sessions/attendance append-only, não alterado) | Sim — `CLAUDE.md` §32 | F-016 (`DONE`) | Médio — mitigado: convite nunca cria membro direto, só após aceite; papel GM continua exclusivo (mesma invariante do fluxo antigo) | Sim — `social_invites` (migration 0028, aditiva) | `DONE` | `DONE` | `DONE` (3 integration + parte do E2E) | `DONE` (BATCH8) |
| F-019 | Notificações in-app (D1, zero-cost) | Implementado para todos os eventos de F-016+F-018 (pedido recebido/aceito, convite recebido/aceito); `notifications.kind` ampliado via rebuild seguro (migration 0028 — tabela sem FK de entrada, vazia em produção) | Sim — decorre diretamente de F-016/F-018 | F-016 (`DONE`), F-018 (`DONE`) | Baixo — CRUD simples, sem realtime obrigatório | Sim — `notifications` (0027 criada, 0028 amplia `kind`) | `DONE` | `DONE` (sino na sidebar + dropdown) | `DONE` | `DONE` (BATCH8 — vertical Social/F-016..F-019 completa) |
| F-020 | Character Sheet Engine — motor genérico + templates por sistema | Implementado como domínio próprio: `src/domain/sheets.ts` (graduado de `src/domain/future/character-sheet.ts`, contrato `validateSheet`/`SheetTemplate` preservado — `tests/unit/future-architecture.test.ts`), `sheet_templates`+`character_sheets` (migration 0029, aditiva). Modelo declarativo (TEXT/NUMBER/BOOLEAN/CHOICE), `world_id` opcional (Personagem não exige World — Vault-first), `version` incrementada quando os campos mudam (ficha antiga fica `outdated:true` sem invalidar retroativamente). Aplica-se a CHARACTER/NPC; leitura segue `authorizedEntity` (visibilidade da entidade), escrita `ownedEntity` (mesmo limite de "sem co-edição" já documentado em `vault.ts`) | Sim — `CLAUDE.md` §31 (domínio próprio: template + campos tipados + browser renderer) | Nenhuma | Médio — mitigado: TEMPLATE_FIELDS_IN_USE bloqueia remover campo com valor em uso, TEMPLATE_WORLD_MISMATCH impede vincular modelo de outro World, entity_type restrito a CHARACTER/NPC | Sim — `sheet_templates`,`character_sheets` (0029, aditiva) | `DONE` | `DONE` (`/app/sheets` modelos + painel/editor na entidade) | `DONE` (5 integration + 1 E2E desktop/mobile) | `DONE` (BATCH9) |
| F-021 | PDF Character Sheets (AcroForm/overlay, editor de template) | Implementado: `sheet_templates.pdf_url`+`pdf_mapping_json` (migration 0031, aditiva). O PDF em si NUNCA passa pelo servidor — só a URL (validada como https pública, mesma política de `coverUrl`) e o mapeamento (metadata) são persistidos; todo fetch/preenchimento/geração roda no navegador via `pdf-lib` (MIT, dependência client-side com import dinâmico para não inflar o bundle principal — ver `src/client/pdf/sheet-pdf.ts`). Suporta ACROFORM (nome de campo detectável no navegador, "Detectar campos do PDF") e OVERLAY (página/x/y/tamanho de fonte, para PDFs digitalizados sem formulário). `connect-src` da CSP ampliado para `https:` pela mesma razão já aplicada a `img-src` (capas) | Sim — `CLAUDE.md` §31 | F-020 (`DONE`) | Alto — risco de licenciamento (nunca redistribuir PDF oficial protegido — mitigado: zero armazenamento/proxy do conteúdo, só referência); processamento de PDF client-side por custo zero (mitigado: pdf-lib é MIT/gratuito, roda 100% no navegador) | Sim — `sheet_templates.pdf_url`,`pdf_mapping_json` (0031, aditiva) | `DONE` | `DONE` (edição de modelo agora existe — gap do F-020 corrigido; detecção de campos + mapeamento por campo + download) | `DONE` (1 unit + 2 integration + 2 E2E desktop/mobile, incluindo fluxo real de detecção/preenchimento com PDF gerado em memória) | `DONE` (BATCH11) |
| F-022 | Vault avançado — reutilização entre Worlds/Campaigns/Adventures (LINK vs COPY/FORK explícito) | Implementado (BATCH12): reuso "entre Campaigns/Adventures" já estava satisfeito por `campaign_entities` (migration 0007). Gap real era "entre Worlds" — `world_entity_links` (world_id,entity_id, migration 0032, aditiva) resolve com LINK many-to-many, nunca altera `world_id` original nem autorização (`worldEntityDiscoveryPredicate`, sempre combinado com `entityAuthorizationPredicate`, nunca sozinho); Wiki/Vault contextual/busca/Grafo passam a considerar link OU world_id dono — Timeline deliberadamente excluída (evento é interpretado sob o calendário de UM World, reprojeção entre calendários é redesenho maior, fora do v1). `POST /vault/:id/fork` (COPY explícito, reaproveita buildEntityCreateStatements/validateReferences — um fork É um create normal) cria entidade independente, nunca copia parentEntityId nem statBlock de criatura, nunca sincroniza de volta | Sim — `CLAUDE.md` §25 (Vault é reutilizável, entidades não morrem com a Campaign) | Nenhuma | Médio — mitigado: LINK nunca modifica `world_id`/autorização (testado explicitamente: Player não ganha acesso a entidade GM_ONLY via link), FORK sempre cria entidade nova sem vínculo de volta, owner-only em ambos (nunca cross-account) | Sim — `world_entity_links` (0032, aditiva); fork não precisou de tabela nova | `DONE` | `DONE` (painel "Worlds vinculados" + botão "Duplicar" na entidade) | `DONE` (6 integration + 1 E2E desktop/mobile) | `DONE` (BATCH12) |
| F-023 | Vault system-aware (campos customizados por Game System sem tabela por RPG) | Implementado: `sheet_templates.game_system_id` (migration 0030, aditiva), mutuamente exclusivo com `world_id` (validado na rota, não via CHECK). Compatibilidade de uma entidade com um modelo de Game System é derivada de `worlds.default_rpg_id → rpgs.publication_id → publications.game_system_id` (cadeia já existente, nenhuma coluna nova em vault_entities) — um modelo escopado a um sistema vale para qualquer entidade cujo World tenha um Rpg padrão daquele sistema, sem precisar de tabela por RPG. `GET /sheets/entities/:id/templates` é a única fonte de verdade de compatibilidade, reaproveitada pelo PUT de vínculo | Sim — decorre de F-020 (mesma necessidade de campos declarativos) | F-020 (`DONE`, reaproveitar `sheet_templates`) | Médio — mitigado: mutual exclusão validada no schema Zod, `INVALID_GAME_SYSTEM` rejeita ID inexistente, `TEMPLATE_INCOMPATIBLE` nunca deixa vincular fora do escopo | Sim — `sheet_templates.game_system_id` (0030, aditiva) | `DONE` | `DONE` (seletor "Game System" no formulário de modelo, mutuamente exclusivo com World) | `DONE` (2 integration + 1 E2E estendido) | `DONE` (BATCH10) |
| F-024 | One-Shots como conceito explícito | Implementado (BATCH13): `campaigns.session_mode` (`CAMPAIGN`\|`ONE_SHOT`, migration 0033, aditiva, default `CAMPAIGN`) — confirmado como especialização segura de Campaign (mesmo padrão de F-013/F-014), sem domínio/tabela nova. Filtro `GET /campaigns?sessionMode=` para a visão "Meus One-Shots"; badge na listagem, campo "Formato" no criar/editar, linha "Formato" no resumo da campanha | Sim | Nenhuma | Baixo — especialização de Campaign, sem tabela nova | Sim — `campaigns.session_mode` (0033, aditiva) | `DONE` | `DONE` | `DONE` (2 integration + E2E estendido) | `DONE` (BATCH13) |
| F-025 | Adventures aprofundadas (acts/scenes/encounters/handouts estruturados) | Implementado (BATCH13): `adventure_scenes`+`adventure_encounters`+`adventure_scene_entities`+`adventure_handouts` (migration 0034, aditiva), ligadas ao Vault Entity ADVENTURE existente. `adventure_scene_entities` só referencia NPCs/Locations/etc já cadastrados no Vault (mesmo princípio de `campaign_entities` — nunca duplica). Handouts aceitam texto livre e/ou referência a um External Resource já existente do mesmo World (F-003), nunca hospedam arquivo. `revealed_at` fica preparado para F-033 (Player View) sem implementar a superfície de jogador ainda. v1 é ferramenta de preparação do GM — tudo owned-only (mesmo modelo do Diário/Journal) | Sim — `CLAUDE.md` §26, pedido explícito de estruturar em acts/scenes/encounters | Nenhuma técnica | Médio — mitigado: `adventure_scene_entities` só referencia (nunca duplica), owner-only em tudo, `entity_type` validado (só ADVENTURE tem estrutura) | Sim — 4 tabelas novas (0034, aditiva) | `DONE` | `DONE` (`/app/vault/:id/adventure`) | `DONE` (5 integration + 1 E2E desktop/mobile) | `DONE` (BATCH13) |
| F-026 | Conteúdo oficial/licenciado (provenance, publisher, edition, read-only) | Implementado (BATCH14): `vault_entities` ganhou `content_source` (`USER_CREATED`\|`LICENSED`\|`OFFICIAL_REFERENCE`), `publisher`, `edition`, `license_note`, `content_locked` (migration 0035, aditiva) — arquitetura de proveniência aplicada a QUALQUER Vault Entity (NPC/Location/Item/Lore/Adventure/etc.), nunca copia texto protegido: `OFFICIAL_REFERENCE` é só um ponteiro ("Ver Livro X, p.34"), nunca o texto integral. `content_locked` tem aplicação real no backend — `PATCH /vault/:id` rejeita (409 `CONTENT_LOCKED`) mudar a descrição enquanto travado, exceto na mesma chamada que destrava. Fork (F-022) preserva a proveniência mas nunca herda o travamento | Sim | F-020 (`DONE`), F-025 (`DONE`) | Alto — jurídico/licenciamento (mitigado: nunca copia conteúdo, só marca a origem declarada pelo usuário; travamento é real, não só rótulo de UI) | Sim — `vault_entities.content_source`,`publisher`,`edition`,`license_note`,`content_locked` (0035, aditiva) | `DONE` | `DONE` (fieldset "Proveniência" no formulário + exibição no detalhe) | `DONE` (3 integration + E2E desktop/mobile) | `DONE` (BATCH14) |
| F-027 | Compendium (view agregada Creatures/Items/Lore/Spells/Adventures) | Implementado (BATCH14): `/app/compendium`, view pura de consulta rápida sobre `GET /vault` (mesmo endpoint da página Vault) — zero domínio/tabela nova, confirmando a reavaliação: com F-020 (fichas) e F-025 (Adventures estruturadas) prontos, a view passou a agregar Criaturas (com stat block inline reaproveitando `creature.statBlock` já existente), Itens e Lore, filtrável por World e busca; clicar num card abre a MESMA entidade do Vault (nunca uma cópia) | Sim — decorre de F-020 (`DONE`)/F-025 (`DONE`) | F-020 (`DONE`), F-025 (`DONE`) | Baixo | Não (view sobre dados existentes, confirmado — nenhuma migration neste item) | `DONE` | `DONE` (`/app/compendium`) | `DONE` (1 E2E desktop/mobile — sem backend novo para testar via integration) | `DONE` (BATCH14) |
| F-028 | Files/Handouts/Assets zero-cost | Implementado (BATCH15): `file_assets` (migration 0036, aditiva) + KV namespace próprio `ASSETS_KV` (criado via `wrangler kv namespace create`, Free) — generaliza o padrão já provado por LIB-005/`COVERS_KV` (`docs/library/COVER_STORAGE.md`): bytes no KV, metadata/ownership/quota no D1, magic-bytes real (nunca confia no Content-Type do cliente), nunca base64 em coluna. Diferença deliberada de capas: leitura é SEMPRE owner-only (um asset pode ser handout/mapa privado de mesa, capa é catálogo compartilhado). Anexo opcionalmente ligado a uma Vault Entity (nunca duplica a entidade). Quota de 40 arquivos/conta (controla acúmulo dentro do 1GB total do KV Free). **BATCH21 (Seção 8 do pedido de finalização):** backup REAL dos bytes (não só metadata) via bundle próprio `GET/POST /api/v1/files/backup` (`src/server/routes/files.ts`) — cap de 15MB brutos por bundle (413 explícito acima disso, nunca trunca), content-type sempre resniffado dos bytes reais no restore, entityId só preservado se a entidade existir na conta que restaura; UI em Configurações → "Arquivos anexados" | Sim — `CLAUDE.md` §30 (nunca R2; usar KV como em `COVER_STORAGE.md`) | Nenhuma (reaproveita o padrão do KV de capas) | Médio — mitigado: quota por conta, cleanup best-effort do KV em toda falha/exclusão (nunca deixa órfão), magic-bytes real | Sim — `file_assets` (0036, aditiva) + `ASSETS_KV` (KV, não D1) | `DONE` | `DONE` (painel "Anexos" na entidade + Configurações → "Arquivos anexados") | `DONE` (3 integration + E2E desktop/mobile + 3 integration do bundle de backup) | `DONE` (BATCH15, backup real BATCH21) |
| F-029 | VTT — fundação (Scene/Map/tokens/permissions, sem realtime) | Implementado (BATCH16): `vtt_scenes`+`vtt_tokens` (migration 0037, aditiva) ligados a Campaign (não a World — ferramenta de mesa), fundo reaproveitando um `world_maps` existente (F-002) ou `imageUrl` própria (mesma política zero-fetch do coverUrl). Escrita sempre owner-only (mesmo modelo do resto de `campaigns.ts`). `GET /vtt/:campaignId/live` é o único ponto onde um jogador (campaign_members.user_id ativo) lê a cena ativa — filtrado no servidor, nunca por CSS: só tokens `visible_to_players=1` aparecem, e o payload do jogador NUNCA inclui entityId/entityName/entityType do token (decisão de segurança deliberada contra vazamento de entidade GM_ONLY/PRIVATE via token mal marcado) | Sim — `CLAUDE.md` §29 explicitamente adiado, nunca cancelado | F-002 (reaproveitar mapas), Campaign/Session | Alto — mitigado com o filtro server-side descrito acima | Sim — `vtt_scenes`, `vtt_tokens` (0037, aditiva) | `DONE` | `DONE` (painel "Mesa Virtual" na Campaign) | `DONE` (6 integration + E2E desktop/mobile) | `DONE` (BATCH16) |
| F-030 | VTT — fog of war / visibilidade | Implementado (BATCH16): `fog_enabled`/`grid_cols`/`grid_rows` em `vtt_scenes` + `vtt_fog_cells` esparsa (migration 0038, aditiva — só células reveladas são armazenadas). GM revela/oculta/reencobre célula por célula (grade normalizada sobre a imagem). Barreira real fica em `GET /vtt/:campaignId/live`: com fog ativo, um token só chega ao jogador se `visible_to_players=1` E a célula calculada a partir de x/y estiver revelada — nunca uma máscara CSS por cima da imagem | Sim | F-029 | Alto — mitigado com o filtro server-side descrito acima (igual GM_ONLY, nunca por CSS) | Sim (0038, aditiva) | `DONE` | `DONE` (grade clicável + "Reencobrir tudo") | `DONE` (3 integration + E2E desktop/mobile) | `DONE` (BATCH16) |
| F-031 | VTT — realtime (posições, iniciativa, cena) | Implementado (BATCH17) como polling; **corrigido (2026-08-20)**: a premissa original de que Durable Objects exigiam plano pago estava errada (verificado contra a documentação oficial da Cloudflare em vigor — Durable Objects SQLite-backed estão disponíveis no Workers Free). Reimplementado com realtime real: `VttRoomDO` (um Durable Object por Campaign, `idFromName(campaignId)`) coordena WebSockets; D1 continua a única fonte de verdade de domínio; protocolo tipado (`src/domain/vtt-realtime.ts`) com `HELLO`/`STATE`/`RESYNC_REQUIRED`/`PONG`; rota `GET /api/v1/vtt/:campaignId/realtime` valida sessão+membership ANTES do upgrade, papel (GM/PLAYER) resolvido no servidor; `VttLivePage` prefere WebSocket, cai no polling de 3s (preservado, não removido) como fallback controlado — ver `docs/architecture/VTT_REALTIME_ZERO_COST_AUDIT.md` para a arquitetura completa e os achados reais de implementação | Sim — corrigido para usar o primitivo real da Cloudflare (Durable Objects Free) em vez da alternativa de polling assumida incorretamente como única opção Zero Cost | F-029 (`DONE`) | Baixo — SQLite-backed Durable Objects Free, sem billing automático em overage (erro 1027, nunca cobrança); D1 permanece a única fonte de verdade, sem duplicação de domínio | Sim (Durable Object class `VttRoomDO`, `durable_objects`/`migrations` em `wrangler.jsonc`, aditiva) | `DONE` (`vtt-room-do.ts`, `GET .../realtime`, `notifyRoom` em toda rota mutante de vtt.ts) | `DONE` (`VttLivePage` — WebSocket preferido, polling como fallback, badge "Tempo real"/"Polling de fallback") | `DONE` (21 testes de integração cobrindo GM/Player/Outsider, token oculto, fog, turno, disconnect/reconnect, sequência/resync, dois jogadores; E2E com dois contextos de browser reais provando atualização automática sem reload) | `DONE` (BATCH17 polling + correção 2026-08-20 realtime real) |
| F-032 | Iniciativa/combate system-neutral | Implementado (BATCH17): `vtt_combatants` + `vtt_scenes.combat_active`/`combat_round` (migration 0039, aditiva). Ordem de turno sempre derivada (`initiative DESC, created_at`), nunca uma coluna de posição própria. HP é deliberadamente GM-only nesta v1 (nunca enviado à visão do jogador em `/live`) — reduz risco de meta-gaming, mantém o v1 simples; visibilidade de HP ao jogador fica para uma iteração futura se houver demanda real. Encerrar combate limpa os combatentes (efêmero, mesmo princípio de F-001 de nunca versionar dado de VTT) | Sim | F-029 (`DONE`) | Médio — mitigado: remover o combatente do turno atual sempre passa o turno adiante (nunca deixa a cena "sem ninguém"), token de combatente validado contra a própria cena (nunca cross-scene/cross-account) | Sim — `vtt_combatants` (0039, aditiva) | `DONE` | `DONE` (painel "Combate" na cena — iniciar/avançar turno/ajustar PV/encerrar) | `DONE` (5 integration + E2E desktop/mobile) | `DONE` (BATCH17) |
| F-033 | Player View integrada (character+sheet+handouts+VTT+notificações) | Implementado (2026-08-20): "Minhas Mesas" (`GET /campaigns/mine`) — o jogador descobre campanhas onde é membro ativo sem link do GM. `GET /campaigns/:id/player-home` agrega resumo da campanha + "Meu Personagem" (novo `campaign_members.character_entity_id`, migration 0040 aditiva) + Handouts revelados (reaproveita `adventure_handouts.revealed_at`) + flag de cena ativa. "Meu Personagem" reaproveita `GET /vault/:id` e `GET /sheets/entities/:id` — ambos já aplicavam a barreira `authorizedEntity`/visibility PLAYERS-CAMPAIGN via `campaign_entities`, nenhuma rota de leitura nova precisou ser criada. VTT reaproveita `VttLivePage` (F-031) direto, nenhuma segunda implementação. Notificações contextualizadas (seção 19 da correção) ficaram FORA desta passada — deferidas, não implementadas | Sim — decorre de F-020/F-029/F-019 | F-019 (`DONE`), F-020 (`DONE`), F-029 (`DONE`) | Baixo — nenhuma autorização nova criada, só reaproveitada; `character_entity_id` validado owner+entity_type via `validateCharacterEntity`, mesmo padrão de `validateAdventure` | Sim — `campaign_members.character_entity_id` (0040, aditiva, `ON DELETE SET NULL`) | `DONE` (`GET /campaigns/mine`, `GET /campaigns/:id/player-home`, `characterEntityId` no member CRUD) | `DONE` (`PlayerCampaignsPage`/`PlayerCampaignHomePage`, link global "Minhas Mesas") | `DONE` (5 integration cobrindo IDOR/visibility/handouts revelados/VTT + 1 E2E desktop/mobile) | `DONE` (2026-08-20) — notificações contextuais (seção 19) ficam como débito para BATCH19 |
| F-034 | GM View integrada (prep de scene/mapa/tokens/fog/handout/iniciativa numa tela) | Implementado: `VttPage` já unia scene/mapa/tokens/fog/combate (F-029/F-030/F-032); ganhou a seção "Preparação da Adventure" — resumo (contagem de cenas/concluídas, handouts revelados) reaproveitando `GET /adventures/:id` (F-025) + `EntityFilesPanel` embutido para anexos (F-028), tudo sem sair da tela do VTT, com link para o editor completo quando o mestre precisar de algo mais profundo | Sim — decorre de F-029/F-030/F-032 | F-029 (`DONE`), F-030 (`DONE`), F-032 (`DONE`) | Médio — mitigado: view pura, nenhuma rota nova | Não (agrega domínios existentes) | `DONE` | `DONE` | `DONE` (E2E desktop/mobile — cria Adventure+cena+handout, confere resumo, anexa arquivo inline, abre editor completo) | `DONE` |

## Explicitamente fora do roadmap planejado (não confundir com adiamento)

Nada nesta categoria — diferente da fase 1.0, não há mais itens
"explicitamente fora do produto" além dos que já são impossíveis por
licenciamento (nunca redistribuir conteúdo oficial protegido) ou por
Zero Cost (nunca serviço pago). Esses limites não são features adiadas,
são restrições permanentes do projeto (`CLAUDE.md` §9, §18).

## Ordem de execução (dependência técnica real, não preferência)

Execução real (histórico, não mais planejamento): BATCH6 F-015 → BATCH7
F-016+F-019 (Social base) → BATCH8 F-017+F-018 (Biblioteca social +
convites) → BATCH9 F-020 (Sheet Engine base, `DONE`) → BATCH10 F-023
(Vault system-aware, `DONE`) → BATCH11 F-021 (PDF Character Sheets,
`DONE`) → BATCH12 F-022 (Vault avançado, `DONE`) → BATCH13 F-024+F-025
(One-Shots + Adventures, `DONE`) → BATCH14 F-026+F-027 (conteúdo
oficial + Compendium, `DONE`) → BATCH15 F-028 (Files/Handouts, `DONE`) →
BATCH16 F-029+F-030 (VTT fundação + fog, `DONE`) → BATCH17 F-031+F-032
(realtime inicialmente resolvido como polling Zero Cost + combate,
`DONE` — inclui hardening real de CI: E2E dividido em 2 jobs paralelos,
achado real de flake escalado corrigido na raiz, não mascarado) →
correção de roadmap (2026-08-20): premissa de F-031 estava errada
(Durable Objects SQLite-backed disponíveis no Workers Free) —
reaberto e reimplementado com realtime real via Durable Object +
WebSocket, polling preservado como fallback, `DONE` de verdade — ver
`docs/architecture/VTT_REALTIME_ZERO_COST_AUDIT.md` → BATCH18 F-033+
F-034 (`DONE` — F-033: "Minhas Mesas", "Meu Personagem", Handouts
revelados, entrada na Mesa Virtual, notificações contextuais adiadas
para BATCH19; F-034: "Preparação da Adventure" + anexos F-028
embutidos direto na tela do VTT) → BATCH19 hardening final + F-015
revalidado (`DONE` — CI sobe trace/screenshot como artifact em falha
para investigação real de flake; export v9 cobre 100% dos domínios
criados desde o BATCH6 original — Social, Sheets, world_entity_links,
Adventures estruturadas, Files/Handouts metadata, VTT; restore v1
estendido para cobrir world_entity_links, mesma fronteira documentada
do escopo v1 preservada para o restante — ver
`docs/library/LIBRARY_IMPORT_EXPORT.md`).

`RPG MANAGER — PLANNED_ROADMAP_COMPLETE` alcançado: todo item de F-022
a F-034 está `DONE`, F-015 foi revalidado cobrindo os domínios novos, e
CI/deploy/produção convergem — ver `docs/product/RPG_MANAGER_COMPLETE_STATUS.md`
para o relatório final.

**Causa raiz real do flake de `vault-worlds-flow.spec.ts` encontrada e
corrigida (2026-08-20):** o CI artifact upload (item acima) permitiu
baixar o trace/screenshot da falha pela primeira vez — a evidência
mudou o diagnóstico por completo. O screenshot da falha mostrava a
tela de OUTRO teste (`player-view.spec.ts`), e o `trace.zip` mostrou
apenas 2 frames de screencast em 270s inteiros: o processo de renderer
do Chromium ficou completamente congelado, não um elemento lento.
Causa raiz real: `player-view.spec.ts`, `vtt-live.spec.ts` e
`vtt-realtime.spec.ts` criavam contexts extras via `browser.newContext()`
para cenários multi-conta e nunca os fechavam — com `workers:1` (mesmo
processo/browser para a fila inteira), cada um deixava contexts abertos
(alguns com WebSocket ainda conectado ao Durable Object) acumulando
memória/conexões até travar o browser bem mais adiante na fila.
Corrigido com `await xContext.close()` nos 3 arquivos — CI verde de
primeira depois do fix, sem rerun. Ver `docs/product/MASTER_BACKLOG.md`.

**Polimento adicional encontrado por relato real do usuário
(2026-08-20):** botão de notificações reposicionado (vivia espremido
dentro de `.sidebar-toolbar`, cortado pela scrollbar em telas mais
estreitas — movido para a linha do brand); atalho "Nova mesa única" em
`CampaignsPage` (One-Shot sempre existiu como Formato do formulário de
campanha, F-024, mas sem nenhum atalho visível ninguém descobria a
opção).

## Critério de conclusão

`RPG MANAGER — PLANNED_ROADMAP_COMPLETE` só quando todo item acima
estiver `DONE` ou `BLOCKED` com motivo técnico/legal/financeiro
documentado — nunca `OUT_OF_SCOPE` para itens desta lista.
